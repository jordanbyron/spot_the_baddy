- content_for :header_bottom do
  :javascript
    $(document).ready(function(){
      var latlng = new google.maps.LatLng(39.2957589, -76.661596);
      var myOptions = {
        zoom: 9,
        center: latlng,
        mapTypeId: google.maps.MapTypeId.ROADMAP
      };
      var map = new google.maps.Map(document.getElementById("map"), myOptions);
      #{render :partial => "load_baddies"}
    });

#header
  = image_tag "header.png"
  
%h1 
  One of these locations was recently a crime scene.  Guess which one!
  %span{:id => "score"}= "#{session[:score] || 0} Points"
#map

%div{:align => :center}
  %h2
    Real Crimes, Real Baddies.  Data sourced from spotcrime.com
